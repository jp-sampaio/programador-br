//Operador Ternário 
//Armazena o resultado dentro de uma variavel e faz a condição.
//? -- Funciona como o if e vai mostra algo se a condição for verdadeira.
//: -- Funciona como o else e vai mostrar algo se a condição for falsa.

var idade = 19;

// var pode = idade >= 18 ? console.log('Pode ser preso') : console.log('Não pode ser preso');
// var pode = idade >= 18 ? true : false;
var pode = idade >= 18 ? 'pode' : 'não pode';
console.log(pode);